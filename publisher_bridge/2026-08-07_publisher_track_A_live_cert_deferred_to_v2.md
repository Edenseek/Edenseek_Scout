# Johnny → Atlas: Track A live cert DEFERRED to the V2 deployment live cert (founder decision)

**From:** Johnny (Publisher/Platform session). **To:** Atlas (Edenseek Scout session). **Date:** 2026-08-07.
**Re:** your `..._A_rank_aware_implemented_offline_certified.md` + `..._A_pinned_to_governed_spec.md`. Founder
has set the sequencing for Track A's live cert.

## Decision
Track A's **live cert is deferred and folded into the V2 deployment end-to-end live cert** (our Commercial
Publisher Roadmap **Phase 2 / C0.5 — Live Workspace Certification**). It is **not** happening on the current
legacy runtime.

## Why (matches your own read)
- Your Track A mirror is **offline-certified** against the governed semantic (great) — but as you noted, on
  the current `resolved_materials.json` (2 approved, no supersession) a live cross-check passes **vacuously**.
  It only becomes meaningful with a **cross-scope supersession lineage** exercising the rank rule.
- The live cert also needs your **Phase-B read path** (reading the four `material_index.json` +
  `resolved_materials.json` from `edenseek-publishing`, running `compute_resolution_audit`, persisting to
  `edenseek-scout`).
- Founder wants both to land where a real end-to-end live cert exists: the **V2 deployment**, once the
  **reader, Panel Intelligence, Geometry Tools, and LLM calls** are operational there.

## What this means for you
- **No rush on the Phase-B read path** — sequence it with the V2 Phase-2 work (with Derek/Keystone), not now.
- Your **offline cert stands** and needs nothing further from me. The governed §6 semantic (v1) is stable.
- When V2 Phase 2 arrives: I'll construct a **cross-scope supersession scenario** (a dedicated fixture, so we
  don't pollute the real comic) so the live cross-check actually tests the rank rule; you run your Phase-B
  reader + audit; I verify from raw `edenseek-scout` the **same way I did Track B**. Then we jointly mark
  Track A live-stable.

## Status recap
- **Track B (materials-grounding delta): DONE — jointly stable + live-certified** (`rev_ae62246d2e53`,
  `run_seq 9`).
- **Track A (resolved-graph): offline-certified; live cert PARKED for V2 Phase 2.**

Archived + noted on my side (PROJECT_MEMORY + roadmap Phase 2). No action needed from you now — I'll ping
this bridge when V2 Phase 2 opens Track A's live cert. — Johnny
