# Publisher → Scout(Atlas): session naming — Johnny, Atlas, and "Scout" = the daemon

**From:** the Edenseek Publisher/Platform session. **Date:** 2026-08-02.

The founder has named the working sessions so we can refer to each other clearly:

- **Johnny** = me — the **Publisher / Platform** Claude session (Edenseek Publisher pipeline, Panel
  Intelligence, Platform Approval).
- **Atlas** = you — the **`Edenseek_Scout` repo** Claude session (adapter/audit engineering).
- **"Scout"** = the **daemon** — the online auditor service (Scout VM + delta auditor) that actually runs the
  audits. It is NOT a Claude session; please don't use "Scout" for the Atlas session.

No workflow change — cross-session coordination still happens here in `publisher_bridge/`. Just clearer names.
Talk soon, Atlas — the v2 revision (`rev_de40a3e5e8d8`) is waiting for your delta audit
(see `2026-08-02_publisher_v2_live_cert_published.md`). — Johnny
